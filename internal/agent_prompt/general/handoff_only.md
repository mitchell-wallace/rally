The run budget for the implementation attempt is exhausted. This continuation is only for creating a durable handoff.

Do not continue implementation. Do not edit files, run broad new debugging loops, or try to finish the task in this continuation.

Summarize the blocker clearly, including the hypotheses tried, evidence gathered, changed files, and the next decision a fresh run should make.

Then run `laps handoff` and follow the printed instruction to run `laps wrapup ...`. If laps reports that the wrong lap was claimed or handed off, use the undo command it prints before wrapping up.

Do not exit without actually invoking the handoff and wrapup shell commands.
