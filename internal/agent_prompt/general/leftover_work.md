The working tree contains uncommitted changes left over from a previous run or manual edits. Before starting your assigned work, review these changes:

- **Code changes** MUST be committed — either commit them now as a separate preparatory commit, or fold them into your end-of-run work commit.
- **Docs or config-only changes** MAY be left uncommitted if they are not relevant to your task.

Run `git status` and `git diff` to inspect the leftover changes, then decide how to handle them before proceeding with your task.
